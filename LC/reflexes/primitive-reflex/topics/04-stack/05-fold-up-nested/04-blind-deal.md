# Stack Atom 05 — Nested-structure fold-up · blind-deal bank (DEALER-ONLY · SPOILER)

> **Do not read during learning.** Claude's deal source for the Phase-2 cold exam:
> reserved problems this atom covers but we have NOT solved cold, kept pristine for cold recognition.
> At deal time hand a **bare link only** — never the atom, type, or any note below.
> When a problem here is solved cold, move it to `03-log.md` and delete the row.

| Link | Source | Title | Note |
|---|---|---|---|
| https://leetcode.com/problems/score-of-parentheses/ | LC 856 | Score of Parentheses | fold operator = double / sum siblings; `()`→1, `(A)`→2A, `AB`→A+B. Pure fold-up, no digits |
| https://leetcode.com/problems/number-of-atoms/ | LC 726 | Number of Atoms | fold operator = multiply child counts by group multiplier and merge into parent map. Fold-up with a Map payload — hardest costume |
| https://leetcode.com/problems/basic-calculator-iii/ | LC 772 | Basic Calculator III | SYNTHESIS — full `+ − * /` + parens = #4 (precedence) ⊕ #5 (fold-up). Also in #3 and #4 banks. Late cross-atom cold test |

NOTE: both acquisition reps are done but **guided** — Decode String (394) approach hint-assisted, Basic Calculator I (224) architecture self-derived but precedence/sign/bug assisted (both now in `03-log.md`). The cold ownership certificate for this atom is still owed — a fold-up problem (e.g. 856) derived end-to-end with no hints on a blind deal.
