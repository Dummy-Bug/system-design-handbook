# Math Reflex Drill Log

Daily reps for the math reflex protocol. See `00-protocol.md` for the rules.

## Format

One entry per drill session. Columns:

| Date | Install band | Maintain bands | Facts asked | Misses | Notes |
|------|--------------|----------------|-------------|--------|-------|

## Graduation events

Log here when a band graduates from install → maintenance, with the date and the 3 consecutive-day check that triggered it.

| Band | Install start | Graduated on | Notes |
|------|---------------|--------------|-------|

## Session log

(Entries below, newest at top.)

---

| Date | Install band | Maintain bands | Facts asked | Misses | Notes |
|------|--------------|----------------|-------------|--------|-------|
| 2026-05-26 | card-add (no drill) | — | — | — | Added Powers & Roots `e.3` (`(int)Math.pow(10,i)` silent-success trap → integer-only pow10) + `e.4` (the `2^53 ≈ 9×10¹⁵` exact-integer boundary — the *why*). Topic now 21 cards. Cross-ref added to Digit Ops `f.1`. Born from *Sum of Digit Differences* (1600-1650 #7) where `(int)Math.pow(10,i)` AC'd silently. **Both ◯ not-yet-drilled — queue for next powers-roots install session.** |
| 2026-05-25 | none (revision day) | full diagnostic sweep — all 46 graduated cards, Topics 01-04, mixed order | 46 | 9 hard ✗ + 8 slow ⚠ (63% clean) | Full Q/A log in `drills/2026-05-25.md`. Patterns: count-vs-sum blur (AS c.1, AS b.2, PT c.1), dropped normalizers (/6, /k!). Weakest topic = Arithmetic Sums (6/9 shaky). 3-day re-drill plan set. |
| 2026-05-24 | Topic 04 Digit Operations (a–l) | none (Topics 01-03 maintenance deferred — heavy install load) | 18 cards installed: a.1, a.2, b.1, b.2, b.3, c.1, d.1, d.2, e.1, f.1, f.2, g.1, h.1, i.1, i.2, j.1, k.1, l.1 | b.2 (floating-point trap — full first-principles unpack), g.1 (place-value confusion — backed up and re-derived), k.1 (assumed no repetition — reframed), l.1 (paused twice, slower re-derivation) | All 18 graduated. j.2 parked as low-leverage shortcut. Topic 04 now 18/24 — card m (digit walk, 1700-band) is the next session install. Maintenance debt flagged for tomorrow. |
